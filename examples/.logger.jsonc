{
  // Example config for logger
  "columns": [
    { "key": "client", "path": "$.client" },
    { "key": "path", "path": "$.request.path" }
  ],
  "keybindings": {
    "toggleReverse": ["v"],
    "cycleMergeSort": ["S"]
  }
}
